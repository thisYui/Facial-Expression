# Core deep-learning stack.
# Kaggle usually preinstalls CUDA-enabled torch/torchvision. Check imports first;
# only reinstall PyTorch if Kaggle's CUDA check fails.
torch>=2.2
torchvision>=0.17

# Image processing and data loading.
opencv-python>=4.8
pillow>=10.0
numpy>=1.24
pandas>=2.0

# CLIP tokenizer/runtime dependencies used by code/clip.
ftfy>=6.1
regex>=2023.0
tqdm>=4.65

# Evaluation, metrics, and plotting for CK+48 experiments.
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Optional dataset download helper for Kaggle-hosted CK+48.
kaggle>=1.6
